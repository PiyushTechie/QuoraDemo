<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .post {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
        }
        .user {
            color: #555;
            cursor: pointer;
        }
        .content {
            font-size: 16px;
            line-height: 1.5;
        }
        button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.2s ease-in-out;
        }
        button a {
            color: white;
            text-decoration: none;
        }
        button:hover {
            background-color: #0056b3;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <h1>Quora Posts</h1>
    <% for (post of posts) { %>
        <div class="post">
            <h2 class="user">@<%= post.username %></h2>
            <p class="content"><%= post.content %></p>
            <button><a href="http://localhost:8080/posts/<%= post.id %>" class="detail-link">Check in detail</a></button>
            &nbsp;&nbsp;&nbsp;&nbsp;
            <button><a href="http://localhost:8080/posts/<%= post.id %>/edit" class="detail-link">Edit</a></button>
            <br><br>
            <form method="POST" action="/posts/<%= post.id %>?_method=DELETE">
                <button type="submit">Delete</button>
            </form>

        
        </div>
   <% } %>
   <br><br>
   <button><a href="http://localhost:8080/posts/new">Create New Post</a></button>
   

</body>
</html>